---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <main class="contact-main">
    <div class="contact-container">
      <!-- Left Column - Content -->
      <div class="contact-content">
        <div class="content-header">
          <h1 class="main-title">¿Listo para transformar la gestión de tu empresa?</h1>
          <p class="main-description">
            Contactanos hoy mismo y descubrí todo lo que podemos hacer por vos.
            Te acompañamos paso a paso para evaluar juntos el sistema.
          </p>
        </div>
        
        <div class="benefits-section">
          <h2 class="benefits-title">Habla con nuestro equipo hoy para:</h2>
          <ul class="benefits-list">
            <li class="benefit-item">
              <i class="bx bx-check-circle"></i>
              <span>Entender cómo nuestro software puede cumplir tus necesidades</span>
            </li>
            <li class="benefit-item">
              <i class="bx bx-check-circle"></i>
              <span>Descubrir las capacidades y obtener respuestas a tus preguntas</span>
            </li>
            <li class="benefit-item">
              <i class="bx bx-check-circle"></i>
              <span>Obtener una cotización personalizada para tu institución</span>
            </li>
          </ul>
        </div>
        

      </div>

      <!-- Right Column - Contact Form -->
      <div class="contact-form-section">
        <form class="contact-form" action="https://formsubmit.co/info@heclipse.com.ar" method="POST" target="_blank">
          <input type="hidden" name="_captcha" value="false">
          <input type="hidden" name="_next" value="https://heclipse.com.ar/gracias">
          
          <div class="form-group">
            <label for="name" class="form-label">Nombre *</label>
            <input type="text" id="name" name="name" required class="form-input" />
          </div>
          
          <div class="form-group">
            <label for="email" class="form-label">Email *</label>
            <input type="email" id="email" name="email" required class="form-input" />
          </div>
          
          <div class="form-group">
            <label for="message" class="form-label">Mensaje *</label>
            <textarea id="message" name="message" rows="4" required class="form-input"></textarea>
          </div>
          
          <button type="submit" class="submit-btn">Enviar mensaje</button>
        </form>

        <!-- Contact Information -->
        <div class="contact-info">
          <h3 class="contact-info-title">O contactanos directamente:</h3>
          <div class="contact-info-items">
            <a href="https://wa.me/5493886828566" target="_blank" class="contact-info-item whatsapp-link">
              <i class="fab fa-whatsapp"></i>
              <span>+54 9 3886 82-8566</span>
            </a>
          </div>
        </div>
      </div>
    </div>


  </main>

  <style>
    .contact-main {
      padding-top: 3rem;
      padding-left: 5rem;
      background: var(--primary);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .contact-container {
      width: 100%;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2em;
      align-items: start;
    }

    /* Left Column Styles */
    .contact-content {
      padding-right: 2rem;
    }

    .content-header {
      margin-bottom: 1.5rem;
    }

    .main-title {
      font-size: 3rem;
      font-weight: 700;
      color: var(--bg-white);
      margin-bottom: 1.5rem;
      line-height: 1.1;
    }

    .main-description {
      font-size: 1.25rem;
      color: var(--bg-white);
      line-height: 1.6;
      margin-bottom: 0;
    }


    .benefits-title {
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--bg-white);
    }

    .benefits-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .benefit-item {
      display: flex;
      align-items: flex-start;
      gap: 1rem;
      margin-bottom: 0.5rem;
      padding: 0.5rem 0;
    }

    .benefit-item i {
      color: var(--bg-white);
      font-size: 1.25rem;
      margin-top: 0.25rem;
      flex-shrink: 0;
    }

    .benefit-item span {
      
      color: var(--bg-white);
      font-size: 1.1rem;
      line-height: 1.5;
    }

    /* Right Column Styles */
    .contact-form-section {
      padding: 2.5rem;
      border-top-right-radius: 30px;
      border-top-left-radius: 30px;
      background-color: white;
    }

    .contact-form {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .form-label {
      font-size: 0.9rem;
      font-weight: 600;
      color: var(--text-dark);
    }

    .form-input {
      padding: 0.75rem 1rem;
      border: 2px solid var(--border-light);
      border-radius: 0.5rem;
      font-size: 1rem;
      background: var(--bg-white);
      color: var(--text-dark);
      transition: all 0.3s ease;
      font-family: inherit;
      resize: vertical;
    }

    .form-input:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(46, 125, 100, 0.1);
    }

    textarea.form-input {
      min-height: 120px;
    }

    .submit-btn {
      background: var(--secondary);
      color: var(--text-white);
      border: none;
      border-radius: 0.5rem;
      padding: 1rem 2rem;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      margin-top: 1rem;
    }

    .submit-btn:hover {
      background: var(--primary-dark);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(46, 125, 100, 0.3);
    }

    /* Contact Information */
    .contact-info {
      margin-top: 2rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border-light);
    }

    .contact-info-title {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--text-dark);
      margin-bottom: 1rem;
      text-align: center;
    }

    .contact-info-items {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .contact-info-item {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 1rem;
      background: var(--bg-lighter);
      border: 1px solid var(--border-light);
      border-radius: 0.5rem;
      text-decoration: none;
      color: var(--text-dark);
      transition: all 0.3s ease;
    }

    .contact-info-item:hover {
      background: var(--primary-bg-light);
      border-color: var(--primary);
      transform: translateX(5px);
    }

    .contact-info-item i {
      font-size: 1.2rem;
      color: var(--primary);
      width: 20px;
      text-align: center;
    }

    .contact-info-item span {
      font-size: 0.95rem;
      font-weight: 500;
    }

    .whatsapp-link:hover {
      background: rgba(37, 211, 102, 0.1);
      border-color: #25d366;
    }

    .whatsapp-link:hover i {
      color: #25d366;
    }

    /* Animations */
    .contact-container {
      animation: fadeInUp 0.8s ease-out;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .main-title {
      animation: slideInLeft 0.8s ease-out 0.2s both;
    }

    .main-description {
      animation: slideInLeft 0.8s ease-out 0.4s both;
    }

    .benefits-section {
      animation: slideInLeft 0.8s ease-out 0.6s both;
    }

    .contact-form-section {
      animation: slideInRight 0.8s ease-out 0.3s both;
    }

    @keyframes slideInLeft {
      from {
        opacity: 0;
        transform: translateX(-30px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes slideInRight {
      from {
        opacity: 0;
        transform: translateX(30px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }



    /* Responsive Design */
    @media (max-width: 1024px) {
      .contact-container {
        grid-template-columns: 1fr;
        gap: 3rem;
      }

      .contact-content {
        padding-right: 0;
        text-align: center;
      }

      .main-title {
        font-size: 3rem;
      }

      .benefit-item {
        justify-content: center;
      }
    }

    @media (max-width: 768px) {
      .contact-main {
        padding: 3rem 1rem 0 1rem;
      }

      .main-title {
        font-size: 2.5rem;
      }

      .main-description {
        font-size: 1.1rem;
      }

      .contact-form-section {
        padding: 2rem;
      }
    }

    @media (max-width: 480px) {
      .main-title {
        font-size: 2rem;
      }

      .contact-form-section {
        padding: 1.5rem;
      }

      .form-input {
        padding: 0.6rem 0.8rem;
      }

      .benefit-item span, .benefits-title {
        text-align: left;
      }
    }
  </style>
</Layout>

 
 