---
// src/components/Contact.astro
---

<section class="section cta-section" id="contacto" data-aos="fade-up">
	<div class="container">
		<div class="two-column-layout">
			<!-- Columna izquierda - Texto -->
			<div class="content-column">
				<h2 class="cta-title">
					¿Listo para transformar <br/> la gestión de tu empresa?
				</h2>
				<p class="cta-subtitle">
					Contactanos hoy mismo y descubrí todo lo que podemos hacer por vos. <br/>
					 Te acompañamos paso a paso para evaluar juntos el sistema.
				</p>
			</div>

			<!-- Columna derecha - Formulario -->
			<div class="form-column">
				<form class="contact-form" action="https://formsubmit.co/mcsilvestri19@gmail.com" method="POST" target="_blank">
				<input type="hidden" name="_captcha" value="false">
				<input type="hidden" name="_next" value="https://heclipse.com.ar/gracias">
				<!-- Honeypot anti-spam -->
				<div style="display:none;">
					<label for="website">No completar este campo</label>
					<input type="text" id="website" name="website" tabindex="-1" autocomplete="off" />
				</div>
				<div class="form-row">
					<div class="form-group">
						<label for="name">Nombre</label>
						<input type="text" id="name" name="name" required placeholder="Tu nombre" />
					</div>
					<div class="form-group">
					<label for="email">Email</label>
					<input type="email" id="email" name="email" required placeholder="tu@email.com" />
				</div>
				</div><div class="form-group">
						<label for="message">Mensaje</label>
						<textarea id="message" name="message" rows="4" required placeholder="¿En qué podemos ayudarte?"></textarea>
					</div>
				
				<button type="submit" class="form-submit">Enviar mensaje</button>
			</form>
			</div>
		</div>
	</div>
</section>

<style>
	/* ===== SECCIÓN CTA ===== */
	.cta-section {
		background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
		position: relative;
		overflow: hidden;
	}
	.two-column-layout {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: center;
		padding: 4rem 2rem;
		max-width: 1200px;
		margin: 0 auto;
	}

	.content-column {
		padding-right: 2rem;
	}

	.form-column {
		display: flex;
		justify-content: center;
		position: relative; /* Asegura que el z-index funcione */
		z-index: 2; /* Eleva la columna del formulario sobre el fondo */
	}

	.cta-section::before {
		content: '';
		position: absolute;
		inset: 0;
		background-image: url("https://images.unsplash.com/photo-1576091160550-2173dba999ef?q=80&w=2070");
		background-size: cover;
		background-position: center;
		opacity: 0.1;
		z-index: 1;
	}



	.cta-title {
		font-size: 3rem;
		font-weight: 700;
		color: white;
		margin-bottom: 1.5rem;
		line-height: 1.2;
		text-align: left;
	}

	.cta-subtitle {
		font-size: 1.25rem;
		color: rgba(255, 255, 255, 0.9);
		margin: 0;
		line-height: 1.5;
		text-align: left;
	}

	/* ===== CONTENEDOR FORMULARIO ===== */
	.contact-form-container {
		justify-content: center;
		position: relative;
		z-index: 2;
		
	}

	.contact-form {
		background: rgba(255,255,255,0.15);
		backdrop-filter: blur(10px);
		border-radius: var(--border-radius-lg);
		padding: 2.5rem 2rem;
		box-shadow: 0 8px 32px rgba(0,0,0,0.12);
		width: 100%;
		gap: 1.5rem;
		border: 1px solid rgba(255,255,255,0.18);
		position: relative; /* Asegura que el z-index funcione */
		z-index: 2; /* Eleva el formulario sobre el fondo */
	}

	.form-row {
		display: flex;
		gap: 1.5rem;
	}


	.form-group {
		display: flex;
		flex-direction: column;
		gap: 0.4rem;
	}
	.form-group label {
		color: #fff;
		font-size: 1rem;
		font-weight: 500;
		margin-bottom: 0.1rem;
	}
	.form-group input,
	.form-group textarea {
		border-radius: 8px;
		border: none;
		padding: 0.7rem 1rem;
		font-size: 1rem;
		background: rgba(255,255,255,0.85);
		color: #222;
		resize: none;
	}
	.form-group input:focus,
	.form-group textarea:focus {
		outline: 2px solid var(--primary);
	}
	.form-submit {
		background: var(--secondary);
		color: #fff;
		border: none;
		border-radius: 8px;
		padding: 0.9rem 1.2rem;
		font-size: 1.1rem;
		font-weight: 600;
		cursor: pointer;
		transition: background 0.2s;
		margin-top: 1rem;
		width: 100%;
	}
	.form-submit:hover {
		background: var(--primary-dark);
	}



	/* ===== RESPONSIVE ===== */
	@media (max-width: 768px) {
		.two-column-layout {
			grid-template-columns: 1fr;
			gap: 2rem;
			padding: 1.5rem 1rem;
		}

		.content-column {
			padding-right: 0;
			text-align: center;
			order: 1;
		}

		.form-column {
			order: 2;
		}

		.cta-title {
			font-size: 2.2rem;
			text-align: center;
			margin-bottom: 1rem;
		}

		.cta-subtitle {
			font-size: 1rem;
			text-align: center;
			line-height: 1.6;
		}

		.contact-form {
			padding: 1.5rem 1rem;
			max-width: 100%;
			width: 100%;
			border-radius: 16px;
		}

		.form-row {
			flex-direction: column;
			gap: 1rem;
		}

		.form-group {
			gap: 0.5rem;
		}

		.form-group input,
		.form-group textarea {
			padding: 0.8rem 1rem;
			font-size: 0.95rem;
		}

		.form-submit {
			padding: 0.9rem 1.5rem;
			font-size: 1rem;
		}
	}

	@media (max-width: 480px) {
		.two-column-layout {
			padding: 1rem 0.5rem;
			gap: 1.5rem;
		}

		.cta-title {
			font-size: 1.8rem;
			margin-bottom: 0.8rem;
		}

		.cta-subtitle {
			font-size: 0.9rem;
		}

		.contact-form {
			padding: 1.2rem 0.8rem;
			border-radius: 12px;
		}

		.form-group input,
		.form-group textarea {
			padding: 0.7rem 0.8rem;
			font-size: 0.9rem;
		}

		.form-submit {
			padding: 0.8rem 1.2rem;
			font-size: 0.95rem;
		}
	}
</style>
