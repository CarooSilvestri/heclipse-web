<section class="section contact-section mb-6" id="contacto" data-aos="fade-up">
	<div class="mb-6">
		<h1 class="big-title has-text-white has-text-centered mb-2">Contacto</h1>
		<h2 class="title is-2 has-text-white has-text-centered">
			Tu mensaje puede ser el inicio de una solución. <br />
			Hablemos.
		</h2>
	</div>

	<div class="container">
		<div class="columns is-centered">
			<div class="column is-5">
				<div class="contact-info p-6">
					<span class="icon-text has-text-white">
						<i class="fas fa-envelope"></i>
					</span>
					<div class="contact-info-text">
						<p class="is-size-4 has-text-white m-0"><strong>Email</strong></p>
						<p class="has-text-white m-0">heclipse.software@gmail.com</p>
					</div>
				</div>
			</div>

			<div class="column is-5">
				<div class="contact-info p-6">
					<span class="icon-text has-text-white m-0">
						<i class="fas fa-phone"></i>
					</span>
					<div class="contact-info-text">
						<p class="is-size-4 has-text-white m-0">
							<strong>Teléfono</strong>
						</p>
						<p class="has-text-white m-0">+54 9 3886 82-8566</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.big-title {
		font-size: clamp(2.5rem, 8vw, 75pt);
		text-align: center;
		line-height: 1.2;
	}

	.contact-section {
		background-color: var(--primary);
		color: white;
		border-radius: 8px;
		position: relative;
		padding: 4rem 2rem;
		background-image: linear-gradient(
				rgba(15, 42, 33, 0.85),
				rgba(15, 42, 33, 0.85)
			),
			url("https://images.unsplash.com/photo-1576091160550-2173dba999ef?q=80&w=2070");
		background-size: cover;
		background-position: center;
	}

	.contact-info {
		background: rgba(255, 255, 255, 0.25);
		border-radius: 1rem;
		backdrop-filter: blur(8px);
		display: flex;
		align-items: center;
		gap: 1.5rem;
	}

	.contact-info-text {
		flex: 1;
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}

	.contact-info-text p {
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}

	.icon-text {
		flex-shrink: 0;
		font-size: 2rem;
		display: flex;
		align-items: center;
	}

	@media screen and (max-width: 768px) {
		.contact-section {
			padding: 3rem 1rem;
		}

		.contact-info {
			padding: 1.5rem;
			flex-direction: column;
			text-align: center;
			gap: 0.75rem;
			min-width: 0;
		}

		.contact-info-text {
			width: 100%;
		}

		.contact-info-text p {
			font-size: 0.9rem;
		}

		.contact-info-text p:first-child {
			font-size: 1.25rem;
			margin-bottom: 0.25rem;
		}

		.icon-text {
			font-size: 1.75rem;
			margin-right: 0 !important;
		}

		.big-title {
			font-size: 40pt;
		}
	}
</style>
