---
import gestionImg from '../../assets/gestion-administrativa.png';
---

<section class="hero">
		<div class="hero-body">
			<div class="columns is-vcentered">
				<!-- Columna de texto -->
				<div class="column is-6">
					<div class="hero-content">
						<h1 class="title is-1" data-aos="fade-up" data-aos-delay="100">
							<span class="brand-name">HECLIPSE</span>, donde la salud y el software se entienden
						</h1>
						<h2 class="hero-subtitle" data-aos="fade-up" data-aos-delay="200">
							Software de Gestión Inteligente para tu empresa de salud
						</h2>
						<p class="hero-description" data-aos="fade-up" data-aos-delay="300">
							<strong>Gestión clínica y administrativa</strong> 100% adaptada a vos.<br>
							 Sin moldes. Sin límites. Gestioná <strong>Sanatorios, Clínicas, Hospitales, Centros de Diagnósticos, Laboratorios y Colegios Médicos</strong>, con una plataforma poderosa y moderna.
						</p>
						
						<a href="/contact" class="submit-button" data-aos="fade-up" data-aos-delay="400">
							<span class="button-text">Pedir una Demo</span>
							<span class="icon">
								<i class="bx bx-right-arrow-alt"></i>
							</span>
						</a>
					</div>
				</div>

				<!-- Columna de imagen -->
				<div class="column is-6" data-aos="fade-left" data-aos-delay="200">
					<div class="hero-image-container">
						<img 
								src={gestionImg.src} 
								alt="Gestión administrativa con tecnología"
								class="hero-image"
							/>

					</div>
				</div>
			</div>
		</div>
</section>

<style>
	.hero {
		background: #f8f9fa;
		min-height: 90vh;
	}

	.hero-body {
		display: flex;
		align-items: center;
		margin: auto;
	}

	.hero-content {
		padding-right: 4rem;
		gap: 1rem;
		display: flex;
		flex-direction: column;
		align-items: flex-start;
	}

	.title {
		color: #1a1a1a;
		font-size: 2.8rem;
		font-weight: 800;
		max-width: 100%;
	}

	.brand-name {
		color: var(--secondary);
		font-weight: 900;
	}

	.hero-subtitle {
		color: var(--primary);
		font-size: 1.4rem;
		font-weight: 600;
		max-width: 100%;
		font-family: var(--font-heading);
	}

	.hero-description {
		color: #4a5568;
		font-size: 1.15rem;
		max-width: 100%;
	}

	.hero-description strong {
		color: #1a1a1a;
		font-weight: 600;
	}

	.submit-button {
		background: #4CAF50;
		color: white;
		padding: 1.1rem 2rem;
		font-size: 1rem;
		font-weight: bold;
		cursor: pointer;
		font-family: var(--font-heading);
		white-space: nowrap;
		border-radius: 46px;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 0.75rem;
		width: 50%;
		align-self: flex-end;
	}
	
	.icon {
		font-size: 1.5rem;
	}

	/* Imagen */
	.hero-image-container {
		height: 100%;
	}

	.hero-image {
		height: 500px;
		width: 100%;
		border-radius: 20px;
		box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
	}

	@media screen and (max-width: 768px) {
		.hero-content {
			text-align: center;
			max-width: 100%;
			align-items: center;
		}

		.title {
			font-size: 2.2rem;
			margin-bottom: 1.5rem;
			line-height: 1.2;
		}

		.hero-subtitle {
			font-size: 1.15rem;
			margin-bottom: 1.5rem;
		}

		.hero-description {
			font-size: 1rem;
			margin-bottom: 2rem;
			text-align: center;
		}

		.submit-button {
			width: 100%;
		}

		.hero-image {
			width: 100%;
			height: 350px;
			border-radius: 15px;
			object-fit: cover;
		}
	}

	@media screen and (max-width: 480px) {
		.title {
			font-size: 1.8rem;
			margin-bottom: 1.2rem;
			line-height: 1.25;
		}

		.hero-subtitle {
			font-size: 1rem;
			margin-bottom: 1.2rem;
		}
		
		.hero-description {
			font-size: 0.95rem;
			margin-bottom: 1.8rem;
			line-height: 1.6;
		}

		.hero-content {
			padding: 0
		}

		.submit-button {
			border-radius: 46px;
			padding: 0.9rem 1.2rem;
			font-size: 0.85rem;
			margin-top: 0;
			white-space: nowrap;
		}

		.hero-image {
			height: 280px;
			border-radius: 12px;
		}
	}

	@media screen and (max-width: 360px) {
		.hero-body {
			flex-direction: column;
		}

		.title {
			font-size: 1.6rem;
			margin-bottom: 1rem;
		}

		.hero-subtitle {
			font-size: 0.95rem;
			margin-bottom: 1rem;
		}

		.hero-description {
			font-size: 0.9rem;
			margin-bottom: 1.5rem;
		}

		.hero-content {
			padding-left: 0.5rem;
			padding-right: 0.5rem;
		}

		.submit-button {
			padding: 0.8rem 1rem;
			font-size: 0.8rem;
		}
		
		.hero-image {
			height: 250px;
		}
	}


</style>
