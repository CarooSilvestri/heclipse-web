<section class="hero is-fullheight-with-navbar">
	<div class="hero-body">
		<div class="overlay"></div>
		<div class="hero-content">
			<h1 class="title is-1">Prospera con Soluciones de Salud Excepcionales</h1>
			<p class="subtitle is-4">
				Confía en nosotros para ser tus aliados en alcanzar la salud y el
				bienestar que mereces
			</p>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		overflow: hidden;
		min-height: 100vh;
		border-radius: 8px;
	}

	.hero-body {
		position: relative;
		z-index: 1;
		background-image: url("https://images.unsplash.com/photo-1631217868264-e5b90bb7e133?q=80&w=2091&auto=format&fit=crop");
		background-size: cover;
		background-position: center;
		animation: scaleDown 1s cubic-bezier(0.4, 0, 0.2, 1) forwards;
		height: 100%;
		position: relative;
	}

	.overlay {
		position: absolute;
		bottom: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			to top,
			rgba(0, 0, 0, 0.7) 0%,
			rgba(0, 0, 0, 0.5) 40%,
			rgba(0, 0, 0, 0.2) 70%,
			rgba(0, 0, 0, 0) 100%
		);
	}

	.hero-content {
		position: absolute;
		bottom: 0;
		left: 0;
		padding: 3rem;
		max-width: 800px;
		z-index: 2;
	}

	.title {
		color: #ffffff;
		line-height: 1.1;
		font-size: 4.5rem;
		font-weight: 700;
		margin-bottom: 1.5rem;
		opacity: 1;
	}

	.subtitle {
		color: #ffffff;
		line-height: 1.6;
		max-width: 540px;
		font-size: 1.25rem;
		margin-bottom: 0;
		opacity: 0.95;
	}

	@keyframes scaleDown {
		0% {
			transform: scale(1.2);
		}
		100% {
			transform: scale(1);
		}
	}

	@media screen and (max-width: 768px) {
		.hero-content {
			padding: 1.5rem;
		}

		.title {
			font-size: 2.5rem;
		}
	}
</style>
