<section class="section contact-section" id="contacto">
	<div class="mb-6">
		<h1 class="big-title has-text-white has-text-centered">Contacto</h1>
		<h2 class="title is-2 has-text-white has-text-centered">
			Tu mensaje puede ser el inicio de una solución. <br />
			Hablemos.
		</h2>
	</div>

	<div class="container">
		<div class="columns is-centered">
			<div class="column is-5">
				<div class="contact-info">
					<span class="icon-text has-text-white">
						<i class="fas fa-envelope"></i>
					</span>
					<div class="contact-info-text">
						<p class="is-size-4 has-text-white">Email</p>
						<p class="has-text-white">heclipse.software@gmail.com</p>
					</div>
				</div>
			</div>

			<div class="column is-5">
				<div class="contact-info">
					<span class="icon-text has-text-white">
						<i class="fas fa-phone"></i>
					</span>
					<div class="contact-info-text">
						<p class="is-size-4 has-text-white">Teléfono</p>
						<p class="has-text-white">+54 9 3886 82-8566</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.big-title {
		font-size: clamp(2.5rem, 8vw, 75pt);
		text-align: center;
		line-height: 1.2;
		margin-bottom: 1rem;
	}

	.contact-section {
		background-color: var(--primary);
		color: white;
		border-radius: 8px;
		position: relative;
		padding: 4rem 2rem;
		background-image: linear-gradient(
			rgba(15, 42, 33, 0.85),
			rgba(15, 42, 33, 0.85)
		),
		url("https://images.unsplash.com/photo-1576091160550-2173dba999ef?q=80&w=2070");
		background-size: cover;
		background-position: center;
	}

	.contact-container {
		flex: 1;
		min-width: 300px;
		max-width: 400px;
		padding: 1rem;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.contact-info {
		background: rgba(255, 255, 255, 0.25);
		padding: 1.5rem;
		border-radius: 1rem;
		backdrop-filter: blur(8px);
		height: 100%;
		display: flex;
		align-items: center;
		gap: 1.5rem;
	}

	.contact-info-text {
		flex: 1;
		min-width: 0;
	}

	.contact-info-text p {
		word-break: break-word;
		hyphens: auto;
	}

	.icon-text {
		flex-shrink: 0;
		font-size: 2rem;
		display: flex;
		align-items: center;
	}

	.button {
		border-radius: 0.5rem;
		transition: transform 0.3s ease;
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 0.5rem;
	}

	.button:hover {
		transform: translateY(-2px);
	}

	.button .icon {
		margin-left: 0.5rem;
	}

	@media screen and (max-width: 768px) {
		.contact-section {
			padding: 3rem 1rem;
		}

		.contact-info {
			padding: 1.5rem;
			flex-direction: column;
			text-align: center;
			gap: 0.75rem;
		}

		.icon-text {
			font-size: 1.75rem;
			margin-right: 0 !important;
		}

		.contact-info-text p {
			font-size: 1rem;
		}

		.contact-info-text p:first-child {
			font-size: 1.25rem;
			margin-bottom: 0.25rem;
		}

		.big-title {
			font-size: 40pt;
		}
	}
</style>
